import 'package:flutter/material.dart';

class OldFeedbackScreen extends StatelessWidget {
  const OldFeedbackScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
