import 'package:flutter/material.dart';

class CoachRoutineScreen extends StatelessWidget {
  const CoachRoutineScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Routine'),
      ),
    );
  }
}
