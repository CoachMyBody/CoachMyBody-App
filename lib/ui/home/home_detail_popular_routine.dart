import 'package:flutter/material.dart';

class DetailPopulatRoutineScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SafeArea(
        child: Text('여기는 디테일창'),
      ),
    );
  }
}
